import type { Meta } from '@storybook/react';
import AuthBlock from './AuthBlock';

const meta: Meta<typeof AuthBlock> = {
  title: 'Example/AuthBlock',
  component: AuthBlock,
};

export default meta;

export const Default = {};
